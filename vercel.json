{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma db push && next build",
  "crons": [
    {
      "path": "/api/cron/close-periods",
      "schedule": "0 3 * * *"
    }
  ]
}